# Legacy 100xfenok.pages.dev → canonical Cloudflare Worker (OpenNext) domain.
# This tiny folder is the Pages "build output directory" so the deploy stays well
# under the 20,000-file asset limit (the repo root has 20k+ files and fails to build).
# 301 every legacy path to the live product domain.
/*  https://100xfenok.etloveaui.workers.dev/:splat  301
