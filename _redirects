# Retire the legacy Cloudflare Pages site (100xfenok.pages.dev).
# This Pages project git-serves the repo root with no build; the live product is
# the Cloudflare Worker (OpenNext) at 100xfenok.etloveaui.workers.dev.
# 301 every legacy path to the canonical Worker domain to kill duplicate content.
/*  https://100xfenok.etloveaui.workers.dev/:splat  301
