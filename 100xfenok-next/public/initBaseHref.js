(() => {
  // Next.js version: no-op. Legacy HTML files use `window.baseHref || './'` fallback,
  // which resolves to correct relative paths (e.g., /100x/data/, /alpha-scout/data/).
  // Original initBaseHref.js is in source/100xFenok/ for GitHub Pages /100xFenok/ prefix.
})();
