name: Invalid Max Queue With Cancellation

on:
  workflow_dispatch:

concurrency:
  group: fixture-writer
  cancel-in-progress: true
  queue: max

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo noop
